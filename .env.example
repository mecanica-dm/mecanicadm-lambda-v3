# Arquivo de exemplo para desenvolvimento LOCAL (NUNCA versionar valores reais).
# No ambiente gerenciado, esses valores vêm do SSM Parameter Store.

AWS_REGION=us-east-1
JWT_SECRET=change-me
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mecanicadmdb
DB_USER=admin
DB_PASSWORD=admin
