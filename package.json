{
  "name": "mecanicadm-lambda-v3",
  "version": "1.0.0",
  "description": "Lambda serverless de validação de CPF e emissão de JWT para a API da oficina",
  "private": true,
  "scripts": {
    "deploy:prod": "serverless deploy --stage prod"
  },
  "devDependencies": {
    "serverless": "^3.38.0",
    "serverless-python-requirements": "^6.0.1"
  }
}
